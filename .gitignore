# Python
.venv/
__pycache__/
*.pyc
.env

# LeRobot outputs — datasets, recordings, checkpoints are too big for git
outputs/
wandb/

# Muscle Memory derived data / indexes / models
work/
work-fixture/

# SO-101 URDF + meshes — cloned on demand (keyboard_ee_teleop.py prints the command)
SO-ARM100/

# Xcode user state
xcuserdata/

# World Context dataset lives on the flash drive / local copy, never in git
WORLD_CONTEXT_EXPLORER_V3/

# OS
.DS_Store

# Licensed font binaries stay local — demo machines copy them in manually
web/fonts/
